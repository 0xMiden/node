[[entries]]
category   = "fixed"
components = ["network-monitor", "node", "ntx-builder", "remote-prover", "validator"]
summary    = "Fixed OpenTelemetry trace exports when the OTLP endpoint uses TLS."
